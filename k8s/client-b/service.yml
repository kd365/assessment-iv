apiVersion: v1
kind: Service
metadata:
  name: park-clustering-api-svc
  namespace: client-b
spec:
  selector:
    app: park-clustering-api
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
