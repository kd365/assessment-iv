apiVersion: v1
kind: Service
metadata:
  name: legal-nlp-api-svc
  namespace: client-c
spec:
  selector:
    app: legal-nlp-api
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
