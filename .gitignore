# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
myenv/
.env

# Prepared data output (regenerated by scripts)
prepared_data/
endpoint_name.txt
feature_count.txt
scale_pos_weight.txt

# Data files (download locally, don't commit large files)
sagemaker/*/data/*.csv
sagemaker/*/data/*.xlsx
sagemaker/*/prepared_data/*.csv
sagemaker/*/prepared_data/*.protobuf
*.tar.gz

# Model artifacts
*.pkl
*.joblib
*.pt
model.tar.gz
model_artifact.txt
training_job_name.txt
feature_dim.txt

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl

# Node / React
dashboard/node_modules/
dashboard/build/
dashboard/.env

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Secrets - NEVER commit these
*credentials*
*secret*
*.pem
*.key
